export function AdminSidebar() {
  const sections = ["Dashboard", "Packages", "Departures", "Bookings"];

  const handleNavigaion = (section: string) => {
      
  }

  return (
    <aside className="min-w-72 min-h-screen bg-white border border-2 rounded-2xl">
      
    </aside>
  );
}
